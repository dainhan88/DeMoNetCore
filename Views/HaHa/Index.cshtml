@model IEnumerable<DeMoMVCNetCore.Models.HiHi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HiHIname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HiHIname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.CategoryID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HiHiId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HiHiId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HiHiId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
